{
  "name": "estranged-lfs-bunnynet",
  "version": "1.0.0",
  "description": "Git LFS batch API server running as a bunny.net edge script",
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "dev": "node esbuild.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@bunny.net/edgescript-sdk": "^0.11.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}